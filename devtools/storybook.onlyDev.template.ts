export const storybookOnlyDevTemplate = `<div class="__storybook-section__" style="line-height: 1.5em; text-align: center;">
  Unfortunately, preview for this block works ONLY on the DEV environment.<br />It use <code>@nuxt/content</code> plugin, which does not work with Storybook :(
</div>`
